import Layout from "@/components/Layout";
import ProductForm from "@/components/ProductForm";

const New = () => {
    return (
        <Layout>
            <ProductForm formName={'Add Product'}/>
        </Layout>
    )
};

export default New;